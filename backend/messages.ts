// dontfall/backend/messages.ts
// Placeholder for message validation utilities.
// The actual validation logic (e.g., schema checks) will be implemented later.
// For now we simply export the message type definitions from
// `types.ts` so other modules can import them without circular
// imports.

export type {
  InputMessage,
  ServerMessage,
} from "./types.ts";
